#include<bits/stdc++.h>
using namespace std;

int main() {

#ifndef ONLINE_JUDGE
	freopen("input.txt", "r", stdin);
	freopen("output.txt", "w", stdout);
#endif

	// code

	// int n;
	// cin >> n;

	// int a[n];
	// for (int i = 0; i < n; i++) {
	// 	cin >> a[i];
	// }

	// sort(a + 0, a + n);

	// int index_ub = upper_bound(a, a + n, 5) - a;

	// int index_lb = lower_bound(a, a + n, 5) - a;

	// cout << index_lb << " " << index_ub;

	// cout << "ferq of " << 5 << " is " << index_ub - index_lb << '\n';

	// cout << upper_bound(a, a + n, 5) - lower_bound(a, a + n, 5);

	// if (binary_search(a, a + n, 5)) {
	// 	cout << "exist";
	// }
	// else {
	// 	cout << "not exist";
	// }

	// cout << count(a, a + n, 5);

	// n = unique(a, a + n) - a;

	// for (int i = 0; i < n; i++) cout << a[i] << " ";

	// cout << n;

	// cout << *max_element(a, a + n);
	// cout << *min_element(a, a + n);

	// next_permutation

	// anagram
	// string s, t;
	// sort(s.begin(), s.end());
	// sort(t.begin(), t.end());

	// if (s == t) cout << "anagram";


	// sort(a, a + n);
	// reverse(a, a + n);


	// sort(a.rbegin(), a.rend());

	// sort(a, a + n, greater<int>());

	// sort(a, a + n, [&](int x, int y) {
	// 	return x > y;
	// });


	// int n;
	// cin >> n;
	// pair<int, int> p[n];

	// for (int i = 0; i < n; i++) {
	// 	cin >> p[i].first >> p[i].second;
	// }

	// sort(p, p + n, [&](pair<int, int> x, pair<int, int> y) {
	// 	if (x.first == y.first) return x.second > y.second;
	// 	return x.first < y.first;
	// });

	// int n;
	// cin >> n;

	// priority_queue<pair<int, int>> Q;

	// for (int i = 0; i < n; i++) {
	// 	int x, y;
	// 	cin >> x >> y;
	// 	Q.push({ -x, y});
	// }

	// while (!Q.empty()) {
	// 	cout << -Q.top().first << " " << Q.top().second << '\n';
	// 	Q.pop();
	// }



}